export type Cat = {
  id: string;
  name: string;
  type: string;
  race: string;
  description: string;
  hairType: string;
  origin: string;
  behaviour: string;
  imageURL: string;
};

export type Dog = {
  id: string;
  name: string;
  type: string;
  race: string;
  description: string;
  hairType: string;
  origin: string;
  size: string;
  imageURL: string;
};
